{
  "acceptance_rule": "valid primary improves over the accepted best by more than 0.002",
  "changes": [
    {
      "new_text": "{\"variant\": \"history_pairwise\"}\n",
      "old_text": "",
      "path": "experiments/configs/integrated-model-evidence/E002-active-variant.json"
    }
  ],
  "editable_paths": [
    "experiments/configs/integrated-model-evidence/E002-active-variant.json"
  ],
  "expected_cost": "CPU-only; full train and validation split",
  "expected_signal": "GAUC and nDCG@5 produce a higher validation primary",
  "experiment_type": "offline_recommendation_ranking",
  "fallback": {
    "batch_size": 4096
  },
  "feature_flags": {
    "pairwise_loss": true,
    "time_safe_history": true
  },
  "hypothesis": "Past-only user/item statistics and time fields should improve ranking under drift.",
  "iteration": 2,
  "model_name": "numpy-factorization-machine",
  "params": {
    "batch_size": 8192,
    "epochs": 40,
    "k": 16,
    "l2": 1e-06,
    "lr": 0.001,
    "max_pairs_per_epoch": 0,
    "negative_per_positive": 1,
    "patience": 4
  },
  "parent_run_id": "integrated-model-evidence-E001",
  "rationale": "Three paired seeds showed a mean +0.002220 primary gain over pointwise FM. History: 0 failed, 1 rejected; evidence score 0.0018.",
  "requested_tool": "run_history_pairwise",
  "run_id": "integrated-model-evidence",
  "seed": 0,
  "single_primary_change": "add leakage-safe historical and time fields to pairwise BPR",
  "timeout_minutes": 60.0,
  "validation_protocol": "Train on the official train split, select checkpoints on valid primary only, and never score or expose test to the Agent."
}
